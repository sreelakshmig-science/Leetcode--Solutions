class Solution:
    def isAnagram(self, s: str, t: str) -> bool:

        print(Counter(s))
        retuen Counter(s)==Counter(t)
        
        if len(s)!=len(t) : return False
        li = [0]*26

        for i in s:
            li[ord(s[i])-97]+=1
            li[ord(t[i])-97]-=1
        return li.count(0)==26
